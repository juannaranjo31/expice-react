import React from 'react';
import ReactDOM from 'react-dom';
import { ExpiceApp } from './ExpiceApp';

ReactDOM.render(
  <React.StrictMode>
    <ExpiceApp />
  </React.StrictMode>,
  document.getElementById('root')
);
